#!/bin/sh

echo "🚀 Running pre-push checks..."

echo "✔️ Type-checking..."
bun run type-check || {
  echo "❌ Type-check failed!"
  exit 1
}

echo "✔️ Building (dry-run)..."
bun run build || {
  echo "❌ Build failed!"
  exit 1
}

echo "✨ All good. Push allowed!"
